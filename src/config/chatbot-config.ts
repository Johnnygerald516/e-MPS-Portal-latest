// Chatbot configuration
export const chatbotConfig = {
  // Set to false to disable the chatbot
  enabled: false,
  
  // Other chatbot configuration options
  openAiModel: 'gpt-4o',
  useLocalQA: true,
  debugMode: false,
};
